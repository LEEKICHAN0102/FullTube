doctype html
html(lang="ko")
  head 
    title #{pageTitle} | FullTube
    link(rel="stylesheet" href="https://unpkg.com/mvp.css")
  body
    header
      h1=pageTitle
    main 
      block content
footer &copy; #{new Date().getFullYear()} FullTube